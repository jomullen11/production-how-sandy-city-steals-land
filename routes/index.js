const sections = require('./sections')
const comments = require('./comments')
const relatedArticles = require('./relatedArticles')

module.exports = {
    sections,
    comments,
    relatedArticles
};